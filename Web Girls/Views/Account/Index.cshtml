@using Web_Girls.Models.ModelsView;
@model List<AccountView>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <div id="page-wrapper">

        <h3 class="page-header; col-lg-12">Thông Tin Tài Khoản</h3>
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">

                    <!-- /.panel-heading -->
                    <div class="panel-body">
                        <div class="table-responsive">
                            <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                                <thead>
                                    <tr>
                                        <th>STT</th>
                                        <th>Mã Hội Viên</th>
                                        <th>Tên Hội Viên</th>
                                        <th>Nhóm Quyền</th>
                                        <th>Khóa</th>
                                        <th>Đăng nhập lần cuối</th>
                                        <th>Tên đăng nhập</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var it in Model)
                                    {
                                    <tr>
                                        <td>@it.STT</td>
                                        <td><a href=/Account/ViewAccount/@it.TenDN>@it.MaHV</a></td>
                                        <td>@it.TenHV</td>
                                        <td>@it.Quyen</td>
                                        @if (it.Khoa)
                                        {
                                            <td class="center"><input type="checkbox" name="Khoa" id="Khoa" checked></td>
                                        }
                                        @if (!it.Khoa)
                                        {
                                            <td class="center"><input type="checkbox" name="Khoa" id="Khoa"></td>
                                        }
                                        <td class="center">@it.EditTime</td>
                                        @*<td class="center" style="list-style-type: none;">
            <li class="dropdown">
                <button aria-expanded="false" aria-haspopup="true" class="btn btn-primary btn-sm" data-toggle="dropdown" type="button"><i class="fa fa-bars"></i></button>
                <ul class="dropdown-menu dropdown-menu-right">
                    <li>
                        <a href="chitiet.html"><i class="fa fa-eye"></i>&nbsp;   Chi tiết</a>
                    </li>
                    <li class="divider"></li>
                    <li>
                        <a href="capnhat.html">
                            <i aria-hidden="true"
                               class="fa fa-save"></i>&nbsp;   Cập nhật
                        </a>
                    </li>
                    <li class="divider"></li>

                    <li>
                        <a id="xoa" onclick="xoa()"><i class="fa fa-edit"></i>&nbsp;   Xóa</a>
                    </li>
                </ul>
            </li>

        </td>*@
                                        <td>@it.TenDN</td>
                                        @* <a> href=/Account/ViewAccount</a>*@
                                    </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                        <!-- /.table-responsive -->

                    </div>
                    <!-- /.panel-body -->
                </div>
                <!-- /.panel -->
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
        <!-- /.row -->
        <!-- /.row -->
        <!-- /.row -->
    </div>
